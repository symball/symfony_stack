app:
    resource: "@AppBundle/Controller/"
    type:     annotation

# Include the standard set of admin pages and actions
admin:
    resource: '@SonataAdminBundle/Resources/config/routing/sonata_admin.xml'
    prefix: /admin

# Include unmatched rule for admin area to handle the data admins
_sonata_admin:
    resource: .
    # The type defines the request handler to use
    type: sonata_admin
    prefix: /admin
